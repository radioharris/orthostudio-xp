.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
dist/
build/
*.egg-info/
.DS_Store
# local data of the former Ortho4XP comparisons (decision 0010): kept on disk, never in git
fixtures/
.orthostudio/
*.log
.vscode/

# Editor and agent scratch (kept locally, not shared)
.claude/

# The address and key of the prepared library: written when the installers are built, from the
# secrets of the repository, never committed (docs/specs/osm-prepared.md).
src/orthostudio/library.json
